<div class="container">
個人ページの作成

	<h2 class="new-section-title">Users</h2>
	<table>
		<% @users.each do |user| %>
			<tr>
				<td><%= attachment_image_tag user, :profile_image, :fill, 100, 100 %></td>
				<td><%= user.name%></td>
				<td><%= user.body%></td>
			</tr>
		<%end%>
	</table>

</div>